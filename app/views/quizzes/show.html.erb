<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1><%= @quizzes %></h1>
      <table class="table table-bordered">
        <tbody>
          <%= @question_one.description %>
          <%# <%= @user_answers.answers %> %>
          <%= @question_two.description %>
          <%= @question_three.description %>
        </tbody>
      </table>
    </div>
  </div>
</div>
